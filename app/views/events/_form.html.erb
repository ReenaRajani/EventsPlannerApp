<%= form_for(@event) do |f| %>
    <% if @event.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

        <ul>
        <% @event.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <p class="field">
      <%= f.label :eventName %><br>
      <%= f.text_field :eventName, :autofocus => true %>
    </p>
    <p class="field">
      <%= f.label :occasion %>
      <%= f.text_field :occasion %>
    </p>
    <p class="field">
      <%= f.label :eventdatetime %>
      <%= f.text_field :eventdate_time %>
    </p>
    <p class="field">
      <%= f.label :address %><br>
      <%= f.text_area :address %>
    </p>
    <p class="field">
      <%= f.label :invitation %><br>
      <%= f.text_area :invitation %>
    </p>
    <p class="actions">
      <%= f.submit :class=>'btn btn-submit full-width' %>
  <% end %>
